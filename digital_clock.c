#include <stdio.h>

int main()
{
    int seconds, minutes, hours;

    printf("Enter seconds value:");  // Prompt for time in seconds
    scanf("%d", &seconds);           // Read value

    printf("\nSeconds value =");
    printf("%d", seconds);            //DEBUG

    return 0;
}
